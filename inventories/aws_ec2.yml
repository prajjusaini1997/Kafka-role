---
plugin: amazon.aws.aws_ec2

regions:
  - us-east-1

filters:
  tag:Role: kafka
  instance-state-name: running

hostnames:
  - private-ip-address

compose:
  ansible_host: private_ip_address

keyed_groups:
  - prefix: tag
    key: tags.Role

strict: false

cache: false
