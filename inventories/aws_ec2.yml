plugin: amazon.aws.aws_ec2

regions:
  - us-east-1

filters:
  tag:Role: kafka
  instance-state-name: running

hostnames:
  - ip-address

keyed_groups:
  - prefix: tag
    key: tags.Role

compose:
  ansible_host: public_ip_address
